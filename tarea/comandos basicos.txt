git( comandos básicos)

ls--> permite saber lo que tenemos en nuestra pc( directorios existentes)
pwd--> saber en que directorio estamos
cd <documents>--> entrar a la carpeta documents
mkdir <clase2 >--> creamos la carpeta clasae2
git branch ----> ramas existentes
git branch <nombre rama>---> crea una rama
git log --oneline --decorate --all --graph---> resumen de las acciones

git checkout <nombre de la rama>--> ir a la rama deseada
git commit -m <rama_practica...mensaje del commit>--->commit de la rama con mensaje

git config --global user.name "nuestro nombre"
git config --global user.email "nuestro email"
git config --global --list

1. ¿Cuál es la diferencia entre algoritmo y programa?

2. Las dos cosas que toda computadora puede hacer son:

3. indicar VoF

El conocimiento declarativo se refiere a declaraciones de hechos____

El conocimiento imperativo se refiere a métodos de "cómo hacer"____

En Una receta para deducir la raíz cuadrada implica adivinar un valor inicial, 
Sin otra receta para saber cómo elegir un número inicial, la computadora no puede generar uno por sí sola._____

4. De un ejemplo de conocimiento declarativo e imperativo.
5. crear un objeto asignado a una variable e imprima:
el identificador
el tipo de datos (entero, decimal,complejo, cadena , etc.)
el valor (el propio dato).